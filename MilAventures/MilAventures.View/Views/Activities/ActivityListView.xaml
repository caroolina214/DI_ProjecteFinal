<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="MilAventures.View.Views.Activities.ActivityListView"
             Background="{StaticResource ColorBlanc}" av:DesignHeight="223.869" av:DesignWidth="671.689">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Activitats"
                       Style="{StaticResource EstilSubtitol}"/>
            <Button Grid.Column="1" Style="{StaticResource BtnSistemaFilled}"
                    Command="{Binding AddCommand}">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Plus" Width="16" Height="16" Margin="0,0,6,0"/>
                    <TextBlock Text="Nova activitat"/>
                </StackPanel>
            </Button>
        </Grid>

        <TextBox Grid.Row="1" Margin="0,0,0,16"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource SearchBarStyle}"/>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Activities}"
                  SelectedItem="{Binding SelectedActivity}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Títol" Binding="{Binding title}" Width="*"/>
                <DataGridTextColumn Header="Categoria" Binding="{Binding Category.code}" Width="120"/>
                <DataGridTextColumn Header="Guia" Binding="{Binding Guide.name}" Width="120"/>
                <DataGridTextColumn Header="Inici"
                    Binding="{Binding init_date, StringFormat=\{0:dd/MM/yyyy\}}" Width="100"/>
                <DataGridTextColumn Header="Fi"
                    Binding="{Binding end_date, StringFormat=\{0:dd/MM/yyyy\}}" Width="100"/>

                <DataGridTemplateColumn Header="Dificultat" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding difficulty, Converter={StaticResource DifficultyConverter}}"
                                       HorizontalAlignment="Center"
                                       FontFamily="{StaticResource FontLabel}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Participants"
                    Binding="{Binding max_participants}" Width="90"/>

                <DataGridTemplateColumn Header="Accions" Width="280">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Style="{StaticResource BtnEditarFilled}"
                                        Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                        Margin="0,0,8,0">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="Pencil" Width="16" Height="16" Margin="0,0,6,0"/>
                                        <TextBlock Text="Editar"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource BtnBorrarFilled}"
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}">
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="Delete" Width="16" Height="16" Margin="0,0,6,0"/>
                                        <TextBlock Text="Eliminar"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>